<div class="question">
    <h1 class="question-title">Q&A</h1>
    <div class="question-container">
        {% for block in section.blocks %}
            <div class="info-box">
                {% if block.settings.question != blank %}
                    <h4 class="subtitle">{{ block.settings.question }}</h4>
                {% else %}
                    <h4 class="subtitle">What kind of trees are there in the world?</h4>
                {% endif %}
                {% if block.settings.answer != blank %}
                    <p class="paragraph">{{ block.settings.answer }}</p>

                {% else %}
                    <p class="paragraph">Lorem ipsum dolor sit amet, consectetur adipiscing elit. 
                                                                Pellentesque fringilla metus ut tellus ultricies egestas. Quisque quis ullamcorper tellus. 
                                                                Maecenas vel nisi consequat, consequat tellus id, dictum dui. Ut id blandit dolor. 
                                                                Cras consequat est non enim cursus, vel laoreet.</p>
                {% endif %}
            </div>
        {% endfor %}
    </div>
</div>

{% schema %}
{
   "name": "Q&A",
   "class": "question-section",
  "presets": [
      {
          "category": "Q&A",
          "name":"Q&A"
      }
  ],
  "blocks": [
      {
         "name": "Q&A Block",
         "type": "question",
         "settings":[
            {
               "type": "text",
               "id": "question",
               "label": "Title"
            },
            {
               "type": "text",
               "id": "answer",
               "label": "Answer"
            }
         ]
      }
  ]
}
{% endschema %}